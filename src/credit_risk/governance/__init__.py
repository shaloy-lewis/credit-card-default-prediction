"""Prediction-only governance and explanation review.

Heavy data-validation modules are intentionally not imported here so the
inference-only container can expose an actionable CLI dependency gate.
"""
